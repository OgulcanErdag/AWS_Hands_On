version: 0.2

phases:
  install:
    commands:
      - node -v
      - npm -v
      - npm i -g @angular/cli@9.0.6

  pre_build:
    commands:
      - npm ci || npm install

  build:
    commands:
      - ng build --configuration production

artifacts:
  files:
    - "**/*"
  base-directory: dist
